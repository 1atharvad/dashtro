server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback so client-side routes (react-router) resolve on refresh.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
